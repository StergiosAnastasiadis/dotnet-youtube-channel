# The Basics of Strings!

Strings are a sequence of characters strung together into a string of characters.

Now below we see the ***$*** sign
```Console.WriteLine($"");```
This is a feature called ***String Interpolation***
It's a way to subtitute literal string text.
```Console.WriteLine($"My friends are {firstFriend} and {secondFriend}");```

So C# one of it's strengths is that it it's a ***Strongly Typed Language***
Unlike JavaScript for example that would have been a ***Runtime Error***
So that error would have happened on .NET run with a language like JS, but in C# the compiler tells you at IDE time, error.
So this tells you about errors before compile before you run.

Now the Dollad sign ***$*** is a modern feature previously we had something like this
```Console.WriteLine("My friend is " + firstFriend + " and" } secondFriend);```

Others things we can do with strings is that we can ***Trim*** them.
